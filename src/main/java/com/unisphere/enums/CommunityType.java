package com.unisphere.enums;

public enum CommunityType {
    PUBLIC,     // Anyone in university can view and join
    RESTRICTED, // Anyone can view, but need approval to join
    PRIVATE     // Hidden, need invitation to join
}